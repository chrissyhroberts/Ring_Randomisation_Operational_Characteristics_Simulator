# Example outputs

This directory is intentionally empty. Run the packaged debug configuration from the repository root to create a small example run below `runs/ring_vax_icc_example_debug/`.

Simulation outputs and checkpoints are excluded from Git by default because production runs can create large numbers of files.

